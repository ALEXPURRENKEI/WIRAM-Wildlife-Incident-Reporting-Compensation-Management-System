# Copy this file to .env for local development.
# Create the database first, then update the username/password if yours are different.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wiram

# true creates demo users/admins in the database on startup. Set false for production.
SEED_DEMO_DATA=true
SERVER_PORT=8080

